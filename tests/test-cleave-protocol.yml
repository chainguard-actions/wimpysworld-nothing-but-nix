name: Test cleave protocol (standard purging, synchronous)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y btrfs-progs lsb-release
          sudo modprobe loop || true

      - name: Pre-check - /nix must not exist
        run: |
          if [ -d /nix ]; then
            echo "FAIL: /nix already exists, cannot run test"
            exit 1
          fi
          echo "PASS: /nix does not exist yet"

      - id: action
        uses: .
        continue-on-error: true
        with:
          hatchet-protocol: cleave
          witness-carnage: 'true'
          root-safe-haven: '2048'
          mnt-safe-haven: '1024'
          nix-permission-edict: 'false'

      - name: Show diagnostics
        run: |
          echo "=== Action outcome: ${{ steps.action.outcome }} ==="
          echo "=== Disk usage ==="
          df -h || true
          echo "=== Loop devices ==="
          sudo losetup -l || true
          echo "=== Expansion dir ==="
          ls -la "${HOME}/.expansion/" 2>/dev/null || echo "expansion dir missing"
          echo "=== Mount info for /nix ==="
          mountpoint /nix && echo "/nix is mounted" || echo "/nix is NOT mounted"
          echo "=== Expansion log (last 100 lines) ==="
          tail -100 "${HOME}/.expansion/expansion.log" 2>/dev/null || echo "no log"

      - name: Check if /nix was successfully mounted
        id: nix-check
        run: |
          if mountpoint -q /nix 2>/dev/null; then
            echo "mounted=true" >> $GITHUB_OUTPUT
            echo "INFO: /nix is mounted, will run full assertions"
          else
            echo "mounted=false" >> $GITHUB_OUTPUT
            echo "INFO: /nix is not mounted - skipping volume-dependent assertions"
          fi

      - name: Assert /nix has substantial space
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          nix_space=$(df -m --output=avail /nix | tail -n 1 | tr -d ' ')
          echo "Available space on /nix: ${nix_space}MB"
          if [ "${nix_space}" -lt 1024 ]; then
            echo "FAIL: /nix has less than 1024MB available (got ${nix_space}MB)"
            df -h
            exit 1
          fi
          echo "PASS: /nix has ${nix_space}MB available"

      - name: Assert purge completion markers exist
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          echo "Checking expansion markers..."
          ls -la "${HOME}/.expansion/" || true

          if [ ! -f "${HOME}/.expansion/holster_done" ]; then
            echo "FAIL: holster_done marker not found"
            exit 1
          fi
          echo "PASS: holster_done marker exists"

          if [ ! -f "${HOME}/.expansion/purge_done" ]; then
            echo "FAIL: purge_done marker not found (expected for cleave protocol)"
            cat "${HOME}/.expansion/expansion.log" 2>/dev/null || true
            exit 1
          fi
          echo "PASS: purge_done marker exists"

          if [ ! -f "${HOME}/.expansion/all_done" ]; then
            echo "FAIL: all_done marker not found"
            cat "${HOME}/.expansion/expansion.log" 2>/dev/null || true
            exit 1
          fi
          echo "PASS: all_done marker exists"

      - name: Assert docker was cleansed
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          if [ ! -f "${HOME}/.expansion/docker_done" ]; then
            echo "FAIL: docker_done marker not found (expected for cleave protocol)"
            exit 1
          fi
          echo "PASS: docker_done marker exists"

      - name: Assert snap was cleansed
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          if [ ! -f "${HOME}/.expansion/snap_done" ]; then
            echo "FAIL: snap_done marker not found (expected for cleave protocol)"
            exit 1
          fi
          echo "PASS: snap_done marker exists"

      - name: Assert apt was cleansed
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          if [ ! -f "${HOME}/.expansion/apt_done" ]; then
            echo "FAIL: apt_done marker not found (expected for cleave protocol)"
            exit 1
          fi
          echo "PASS: apt_done marker exists"

      - name: Assert cleave was completed
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          if [ ! -f "${HOME}/.expansion/cleave_done" ]; then
            echo "FAIL: cleave_done marker not found (expected for cleave protocol)"
            exit 1
          fi
          echo "PASS: cleave_done marker exists"

      - name: Assert TMPDIR is set to /nix/_tmp
        run: |
          if [ "${{ steps.nix-check.outputs.mounted }}" != "true" ]; then
            echo "SKIP: /nix not mounted"
            exit 0
          fi
          echo "TMPDIR: $TMPDIR"
          if [ "$TMPDIR" != "/nix/_tmp" ]; then
            echo "FAIL: expected TMPDIR=/nix/_tmp, got TMPDIR=$TMPDIR"
            exit 1
          fi
          echo "PASS: TMPDIR is set correctly"

      - name: Show final disk usage
        run: |
          echo "Final disk usage:"
          df -h
